import { observer } from "mobx-react-lite";
import React, { } from "react";

const App = observer(() => {

    return (
        <div>
            <p>React 테스트</p>
        </div>
    );
});

export default App;